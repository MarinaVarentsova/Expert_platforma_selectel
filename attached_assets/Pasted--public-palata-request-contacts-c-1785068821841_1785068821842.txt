Нужно восстановить точную бизнес-логику пяти колонок таблицы
public.palata_request_contacts:

- contact_opened_at
- expert_status
- expert_status_updated_at
- failure_reason
- expert_comment

Фактическая база Selectel сейчас этих колонок не содержит, хотя production-код их ожидает.

Ничего пока не исправляй, не создавай миграции, не делай commit и push.

Проведи полный аудит по всему репозиторию.

1. Найди все миграции и SQL-файлы, где эти колонки:
- создаются;
- изменяются;
- читаются;
- записываются;
- используются в WHERE, JOIN, ORDER BY или RETURNING.

2. Для каждой колонки покажи:
- тип данных;
- nullable или not null;
- default;
- ограничения;
- комментарий из миграции;
- файл и номер миграции.

3. Найди все server-side маршруты, которые используют эти колонки.

Для каждого маршрута покажи:
- HTTP method и URL;
- роль пользователя;
- что читает;
- что записывает;
- при каком действии пользователя вызывается.

4. Найди frontend-компоненты и страницы, которые используют эти поля.

Для каждого поля покажи:
- где отображается;
- где редактируется;
- какой пользователь выполняет действие;
- какое состояние интерфейса от него зависит.

5. Найди enum или список допустимых значений для expert_status.

Покажи:
- все возможные значения;
- где они задаются;
- какие переходы между статусами предусмотрены.

6. Найди логику:
- раскрытия контактов;
- принятия заказа экспертом;
- отказа;
- невозможности выполнить заказ;
- комментария эксперта;
- фиксации даты открытия контактов.

7. Проверь, записывались ли эти поля ранее в Supabase через:
- прямые запросы frontend;
- Supabase client;
- RPC;
- server-side SQL;
- триггеры;
- функции базы.

8. Отдельно проверь миграции 020 и 021:
- полный SQL;
- порядок применения;
- зависимости;
- были ли там дополнительные индексы, constraints, enum или backfill данных.

9. Составь итоговую таблицу:

колонка |
назначение |
кто записывает |
кто читает |
маршрут |
frontend |
тип |
default |
допустимые значения |
критичность при отсутствии

10. В конце дай заключение:
- является ли отсутствие каждой колонки потерей действующей функции;
- можно ли безопасно оставить SELECT *;
- какие миграции необходимо применить к Selectel;
- нужно ли переносить старые значения из Supabase;
- какие проверки провести после миграции.

Не вноси изменений.
Не удаляй текущую временную правку SELECT *.
Не делай commit и push.