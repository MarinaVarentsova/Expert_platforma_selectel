Нужно исправить только допуск к созданию заказа по направлению экспертизы.

Не менять AI Gateway.
Не менять PostgreSQL-схему.
Не менять 8 сценариев базы знаний.
Не менять matching и кабинеты пользователей.
Не создавать новые направления.

Подтверждённая текущая проблема:

- в AI передаются все 31 активное направление;
- модель использует собственные обученные знания;
- если направление есть в общем справочнике, заказ создаётся даже при отсутствии сценария в KNOWLEDGE_BASE;
- сервер проверяет только формат UUID;
- ручной выбор направления позволяет обойти AI.

Бизнес-правило текущего этапа:

Платформа принимает заказы только по направлению:

«Строительно-техническая экспертиза»

Создание разрешается только если описание соответствует одному из 8 сценариев строительной базы знаний.

Если описание не соответствует базе знаний, заказ создавать нельзя, даже если модель знает другое направление и оно существует в palata_expertise_directions.

Сделай строго следующее.

1. Найди точный ID направления «Строительно-техническая экспертиза» через backend-запрос к:

public.palata_expertise_directions

Не хардкодить случайный UUID.
Разрешается либо:
- получить ID по точному имени при старте/запросе;
- либо использовать существующую настройку/константу, если она уже есть.

2. В /api/ai-detect-direction передавать модели только одно допустимое направление:

«Строительно-техническая экспертиза»

Не передавать все 31 направления.

3. Измени system prompt так, чтобы модель:

- классифицировала только по 8 сценариям KNOWLEDGE_BASE;
- не использовала общие знания для признания других экспертиз;
- возвращала detected=false, если не найдено соответствие строительным сценариям;
- возвращала detected=false при стоп-факторах;
- не могла вернуть другое направление.

4. Добавь детерминированную проверку до или после AI:

Результат считается допустимым только если:

- detected === true;
- direction_name точно равно «Строительно-техническая экспертиза»;
- confidence >= 0.7;
- matched_markers не пуст;
- описание совпало минимум с одним сценарием KNOWLEDGE_BASE;
- не сработал стоп-фактор.

AI не является единственным источником допуска.

5. В production endpoint создания заказа добавь обязательный server-side guard.

Перед INSERT в public.palata_requests сервер должен проверить:

- expertise_direction_id существует в public.palata_expertise_directions;
- запись активна;
- её name точно равно «Строительно-техническая экспертиза»;
- AI-проверка текущего описания успешно пройдена.

Нельзя доверять expertise_direction_id, который прислал frontend.

6. Нельзя создавать заказ при:

- почерковедении;
- товароведческой экспертизе;
- медицинской экспертизе;
- пожарно-технической экспертизе, если вопрос о причине/очаге пожара;
- юридическом споре без строительных технических вопросов;
- любом другом направлении вне 8 сценариев.

Вернуть HTTP 422:

{
  "success": false,
  "code": "UNSUPPORTED_EXPERTISE",
  "error": "На платформе пока нет экспертов по указанному направлению"
}

7. Убрать обход через ручной dropdown.

Пока разрешено только одно направление:

- либо скрыть ручной выбор;
- либо показывать только «Строительно-техническая экспертиза»;
- но всё равно сохранять серверную проверку.

8. Если OpenAI/Gateway недоступен:

- не разрешать создание заказа автоматически;
- fallback может использовать только локальные маркеры 8 сценариев;
- если локальное совпадение недостаточно — вернуть 422;
- не выбирать направление из общего справочника.

9. Не использовать web_search, tools, browsing или внешние источники.

10. Добавить безопасное логирование:

[AI-DIRECTION-GUARD]

с полями:

- descriptionLength
- detected
- directionName
- directionId
- confidence
- matchedMarkers
- matchedKnowledgeScenario
- stopFactor
- allowed
- rejectionCode

Не логировать полное описание, ключи, токены и персональные данные.

11. Проверить сценарии:

А. «Проверить подпись в договоре на подлинность»
→ HTTP 422
→ заказ не создан.

Б. «После ремонта отваливается плитка, необходимо определить дефекты и стоимость устранения»
→ определяется «Строительно-техническая экспертиза»
→ заказ создаётся.

В. «Определить причину и очаг пожара»
→ HTTP 422 как пожарно-техническая экспертиза.

Г. «Оценить повреждения отделки после залива квартиры»
→ заказ создаётся как строительно-техническая экспертиза.

Д. Передать вручную UUID почерковедческого направления напрямую в endpoint создания
→ HTTP 422.

12. Выполнить:

node --check artifacts/palata/server.js
pnpm --filter @workspace/palata run build

13. Сделать commit:

fix: enforce construction expertise admission

14. Push в main.

В конце показать:

- старую цепочку допуска;
- новую цепочку;
- server-side guard;
- поведение fallback;
- изменения ручного выбора;
- изменённые файлы;
- результаты 5 тестов;
- build;
- commit hash;
- статус push.