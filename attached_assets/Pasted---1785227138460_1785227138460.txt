Проведи полный аудит механизма авторизации и жизненного цикла сессии. Пока ничего не изменяй, не делай commit и не предлагай код.

Контекст:

В production наблюдается проблема у всех пользователей.

Сценарий воспроизведения:

1. Пользователь входит в систему.
2. Работает в личном кабинете.
3. Переходит в другую вкладку браузера либо сворачивает окно.
4. Через некоторое время возвращается.
5. Интерфейс остаётся открытым.
6. Первый же запрос начинает получать:

401 Unauthorized
INVALID_TOKEN

Например:

GET /api/palata/requests/expert/matches
GET /api/palata/action-items/me

После этого большинство запросов начинают возвращать 401, и пользователю приходится заново входить в систему.

Ничего пока не исправляй.

Нужно провести аудит и вернуть отчёт.

Исследуй:

1. Как сейчас устроена авторизация.

- где хранится access token;
- где хранится refresh token;
- используется ли refresh token вообще;
- используются ли cookies или localStorage;
- срок жизни access token;
- срок жизни refresh token.

2. Какие backend endpoint участвуют в авторизации.

Например:

/api/auth/login
/api/auth/me
/api/auth/logout
refresh endpoint (если существует)

Опиши полный жизненный цикл.

3. Как frontend определяет, что пользователь авторизован.

4. Что происходит после получения 401.

Для каждого случая показать цепочку:

запрос →
401 →
какой обработчик →
что делает приложение дальше.

5. Есть ли общий API client / fetch wrapper.

Если есть:

- какие файлы;
- какие функции;
- используют ли его ВСЕ запросы платформы.

Если нет — указать это.

6. Есть ли механизм автоматического обновления access token.

Если есть:

- почему он не срабатывает;

Если нет:

- подтвердить его отсутствие.

7. Проверяет ли приложение сессию:

- при возврате на вкладку;
- при visibilitychange;
- при focus;
- при pageshow.

Если проверяет — показать где.

8. Почему пользователь визуально остаётся авторизованным, но backend уже отвечает INVALID_TOKEN.

9. Есть ли различия между:

- кабинетом клиента;
- кабинетом эксперта;
- админкой;

в обработке авторизации.

10. Есть ли в проекте старая реализация автоматического refresh токена, которая после перехода на новую архитектуру перестала использоваться.

11. Проверить все запросы к backend.

Какие из них используют единый auth client.

Какие выполняются напрямую через fetch.

12. Определить корневую причину.

Не симптомы.

Именно ответить:

Почему после возврата на вкладку приложение не восстанавливает рабочую сессию автоматически.

Верни результат в виде технического отчёта:

- схема текущей авторизации;
- найденные файлы;
- найденные функции;
- последовательность запросов;
- корневая причина;
- варианты исправления (без реализации).

Никаких изменений в коде пока не выполнять.
Никаких commit не делать.