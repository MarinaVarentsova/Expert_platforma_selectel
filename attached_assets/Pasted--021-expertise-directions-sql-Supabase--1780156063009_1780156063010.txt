Миграция `021_expertise_directions.sql` применена в Supabase успешно.

Теперь нужно проверить и завершить переход приложения на единый справочник направлений экспертиз.

ВАЖНО:
Не создавать новую миграцию без необходимости.
Сначала проверить, что уже есть после миграции.

# 1. Проверить БД

Проверить, что существуют:

* `palata_expertise_directions`
* `palata_expert_directions`
* `palata_requests.expertise_direction_id`

Проверить количество направлений:

```sql
select count(*) from palata_expertise_directions;
```

Ожидаем:

```text
31
```

Проверить, что у заказов заполнено:

```sql
select count(*) 
from palata_requests 
where expertise_direction_id is null;
```

Если есть NULL — вывести список таких заказов и причину.

Проверить, что у экспертов есть связи:

```sql
select count(*) from palata_expert_directions;
```

# 2. Проверить формы

Проверить, что направления теперь загружаются из таблицы:

`palata_expertise_directions`

а не из локальных констант.

Проверить страницы:

* регистрация эксперта;
* редактирование профиля эксперта;
* создание заказа;
* редактирование заказа;
* фильтры заказов;
* фильтры экспертов;
* метрики.

# 3. Убрать старые локальные справочники

Проверить и убрать использование старых констант как источника истины:

* `EXPERTISE_TYPES`
* `SPEC_OPTIONS`
* `EXPERTISE_ALIASES`
* `EXPERTISE_LABEL`
* `SPEC_LABEL`
* `SPEC_L`

Они могут остаться только как временный fallback для старых данных, но НЕ должны использоваться в новых формах и matching.

# 4. Проверить создание заказа

При создании нового заказа должно сохраняться:

* `expertise_direction_id`

Старое поле:

* `expertise_type`

можно заполнять только для обратной совместимости, но не использовать как источник истины.

# 5. Проверить профиль эксперта

Эксперт может выбрать несколько направлений.

Сохранять выбор нужно в:

`palata_expert_directions`

Не использовать больше:

`palata_expert_profiles.specializations`

как источник истины для подбора.

# 6. Проверить matching

Matching должен сравнивать:

```text
palata_requests.expertise_direction_id
```

с

```text
palata_expert_directions.expertise_direction_id
```

Больше не использовать:

* fuzzy matching;
* aliases;
* slug matching;
* русские названия как ключ;
* массив `specializations` как основной источник.

# 7. Проверить UI

В интерфейсе отображать человеку:

`palata_expertise_directions.name`

а не slug и не старое значение.

Проверить:

* карточку заказа;
* канбан;
* карточку эксперта;
* профиль эксперта;
* метрики по направлениям.

# 8. Отчёт после проверки

Вернуть:

1. Подтверждение, что миграция видна приложению.
2. Сколько направлений в справочнике.
3. Есть ли заказы без `expertise_direction_id`.
4. Сколько связей эксперт-направление создано.
5. Какие файлы уже переведены на новый справочник.
6. Какие файлы ещё используют старые константы.
7. Matching уже работает по `expertise_direction_id` или ещё по старой логике.
8. Что нужно исправить, если переход выполнен не полностью.
