Нужно разобраться с ЛК эксперта Анны.

Проблема:
после входа под пользователем:

* Подшивайлова Анна
* [podshivailovaann@gmail.com](mailto:podshivailovaann@gmail.com)
* роль: expert

в ЛК эксперта `/expert` отображается пустое состояние:
«Обращений пока нет».

Но мы уже создавали тестовые заказы и связи между Анной и Мариной.

Нужно проверить:

1. Связь пользователя с профилем эксперта

* есть ли запись в `palata_expert_profiles` для `podshivailovaann@gmail.com`;
* совпадает ли `palata_expert_profiles.user_id` с `auth.users.id`;
* совпадает ли `palata_users.id` с `auth.users.id`;
* правильно ли определяется `expert_profile.id`.

2. Связи заказов с экспертом
   Проверить, есть ли записи в:

* `palata_request_matches`
* `palata_request_contacts`

где `expert_id` = `palata_expert_profiles.id` Анны.

Если связей нет — нужно подготовить SQL fix, чтобы связать тестовые заказы с профилем Анны.

3. Логику запроса в `/expert`
   Проверить, какой именно join используется.

ЛК эксперта должен показывать обращения, где Анна:

* была подобрана в `palata_request_matches`;
* или есть контакт в `palata_request_contacts`;
* или назначена администратором.

4. Проверить RLS
   Если данные есть в базе, но не отображаются:

* проверить RLS policies для authenticated;
* эксперт должен иметь возможность читать свои matches, contacts и связанные requests.

5. Профиль эксперта
   Мы уже просили сделать профиль эксперта.
   Сейчас в ЛК эксперта не видно раздела «Профиль эксперта».

Нужно добавить в `/expert` вкладки:

* Мои обращения
* Профиль эксперта

В «Профиль эксперта» показать данные из `palata_expert_profiles`:

* ФИО
* email
* телефон
* регионы работы
* направления экспертиз
* готовность к командировкам
* принимает заказы
* рейтинг
* количество выполненных заказов
* регистрация в Палата судебных экспертов РФ
* номер регистрации
* регистрация в Центр судебных экспертиз
* номер регистрации
* описание опыта

Важно:

* не использовать моковые данные;
* всё брать из Supabase;
* если данные в базе есть, но не отображаются — исправить join/RLS;
* если данных нет — подготовить SQL fix migration.
