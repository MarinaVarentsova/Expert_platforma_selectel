Ничего не менять в коде.

Не делать build, commit, push.

Нужно провести полный аудит функционального блока «Заявка / Заказ».

Цель — определить все таблицы, операции и файлы, которые участвуют:

1. в создании новой заявки заказчиком;
2. в сохранении черновика;
3. в редактировании заявки;
4. в публикации заявки;
5. в загрузке файлов;
6. в сохранении контактов;
7. в подборе экспертов;
8. в назначении эксперта;
9. в смене статуса;
10. в отмене и завершении заявки;
11. в отображении заявки заказчику, эксперту и администратору.

КОД ПОКА НЕ МЕНЯТЬ.

================================
1. Найти точку создания заявки
================================

Найти:

- компонент формы создания заявки;
- функцию submit/save;
- все вызываемые функции;
- все Supabase-запросы;
- все RPC;
- все Storage-запросы;
- все backend endpoints.

Построить цепочку:

форма
→ функция frontend
→ auth
→ INSERT/UPDATE
→ связанные таблицы
→ файлы
→ matching
→ status events
→ redirect

================================
2. Найти все связанные таблицы
================================

Особенно проверить:

palata_requests
palata_request_contacts
palata_request_files
palata_request_matches
palata_status_events
palata_action_items
palata_customer_profiles
palata_users
palata_expert_profiles
palata_expert_directions
palata_expert_regions
palata_expert_certificates
palata_regions
palata_expertise_directions
palata_specialty_codes

Но не ограничиваться этим списком.

Для каждой таблицы указать:

- SELECT / INSERT / UPDATE / UPSERT / DELETE;
- в какой момент используется;
- обязательна ли операция для успешного создания заявки;
- какие поля записываются;
- откуда берётся каждое значение;
- есть ли внешний ключ на request_id;
- что произойдёт, если операция упадёт.

================================
3. Найти все обращения по коду
================================

Найти по всему проекту:

supabase.from("palata_requests")
supabase.from("palata_request_contacts")
supabase.from("palata_request_files")
supabase.from("palata_request_matches")
supabase.from("palata_status_events")
supabase.from("palata_action_items")

Также найти:

.insert(
.upsert(
.update(
.delete(
.rpc(
.storage
request_id
customer_id
expert_id
matched_expert
status

Для каждого найденного обращения показать:

- файл;
- функция;
- примерная строка;
- операция;
- фильтры;
- передаваемые поля;
- ожидаемый результат.

================================
4. Поля формы заявки
================================

Показать все поля, которые вводит заказчик.

Сделать таблицу:

Поле формы
→ таблица
→ колонка
→ преобразование
→ обязательность

Отдельно показать:

- направление экспертизы;
- регион;
- описание;
- сроки;
- бюджет;
- контактные данные;
- файлы;
- согласия;
- дополнительные параметры.

================================
5. Транзакционность
================================

Определить, какие операции должны выполняться одной транзакцией.

Например:

BEGIN
INSERT palata_requests
INSERT palata_request_contacts
INSERT palata_status_events
INSERT palata_action_items
COMMIT

Отдельно указать операции, которые нельзя включить в PostgreSQL-транзакцию:

- загрузка файлов в Storage;
- внешние API;
- email;
- подбор экспертов.

Показать текущую обработку частичных ошибок.

================================
6. Matching
================================

Показать:

- когда запускается подбор экспертов;
- какой файл и функция;
- какие таблицы читает;
- куда записывает результат;
- создаёт ли palata_request_matches;
- вызывается ли matching сразу после создания заявки или позже;
- зависит ли создание заявки от успешного matching.

================================
7. Статусы
================================

Показать полный жизненный цикл заявки:

draft
published
matching
expert_selected
in_progress
completed
cancelled

Использовать реальные значения из текущего кода и enum БД.

Для каждого перехода указать:

- кто может выполнить;
- какая таблица обновляется;
- создаётся ли запись в palata_status_events;
- создаётся ли action item;
- отправляется ли email.

================================
8. Файлы
================================

Показать текущую логику:

- куда загружается файл;
- какой bucket;
- что записывается в palata_request_files;
- хранится ли public URL или storage path;
- как файл удаляется;
- что произойдёт, если файл загрузился, а INSERT заявки упал;
- что произойдёт, если заявка создалась, а файл не загрузился.

================================
9. Схемы таблиц
================================

По schema/migration-файлам показать фактические поля новой PostgreSQL для:

palata_requests
palata_request_contacts
palata_request_files
palata_request_matches
palata_status_events
palata_action_items

Для каждой:

- column_name;
- data_type;
- is_nullable;
- default;
- foreign keys;
- unique constraints;
- enums.

Сравнить с полями, которые сейчас использует Supabase-код.

================================
10. Итоговая карта переноса
================================

Сделать итоговую таблицу:

Шаг процесса
→ текущий файл
→ текущая таблица Supabase
→ операция
→ новая таблица PostgreSQL
→ будущий backend endpoint
→ нужна ли транзакция
→ риск

Разделить будущий перенос на этапы:

Этап A — создание заявки и контактов;
Этап B — файлы;
Этап C — matching;
Этап D — статусы и action items;
Этап E — чтение заявок в кабинетах;
Этап F — редактирование и завершение.

В конце дать рекомендацию:

- какие таблицы необходимо переключать одновременно;
- какие можно переключать отдельно;
- какой минимальный безопасный первый этап;
- какие файлы будут затронуты.

Ничего не исправлять.
Ничего не переносить.
Нужен только полный технический аудит функционального блока «Заявка».