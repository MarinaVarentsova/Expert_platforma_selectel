Нужно создать реалистичные тестовые данные для workflow между:

1. Заказчик:
   Марина Варенцова
   `varentsovsmv@gmail.com`

2. Эксперт:
   Анна Подшивайлова
   `podshivailovaann@gmail.com`

Цель:
наполнить систему живыми тестовыми заказами, статусами, событиями и взаимодействиями, чтобы:

* проверить канбан;
* проверить ЛК;
* проверить workflow;
* проверить метрики;
* проверить карточки заказов.

Нужно создать SQL seed / migration с тестовыми сценариями.

Создать 8–12 заказов с разными статусами:

1. NEW

* новый заказ без подбора

2. MATCHING

* заказ в подборе

3. EXPERT_SELECTION

* заказ с подобранными экспертами

4. CONTACTS_OPENED

* контакты открыты между заказчиком и экспертом

5. CAN_START_FROM

* эксперт указал дату начала

6. IN_WORK

* заказ взят в работу

7. COMPLETED

* заказ завершён

8. DECLINED

* эксперт отказался

9. REPEAT_MATCHING

* все эксперты отказались, пошёл новый подбор

Для заказов использовать:

* разные направления экспертиз;
* разные регионы;
* разные даты;
* разные срочности.

Для Анны:

* часть заказов завершены;
* часть в работе;
* по части отказ;
* по части только открыты контакты.

Добавить:

* `palata_request_matches`
* `palata_request_contacts`
* `palata_status_events`
* рейтинги
* комментарии
* completed_orders_count
* тестовые email events

Для COMPLETED заказов:

* добавить оценки:

  * заказчик → эксперт
  * эксперт → заказчик

Добавить realistic timeline:

* created_at
* updated_at
* события по времени
* progression статусов

Важно:

* использовать существующие auth/users/profile записи;
* не создавать дубликаты пользователей;
* все связи должны быть корректными;
* не ломать текущий auth workflow;
* использовать реальные UUID пользователей из базы.

После создания данных:

1. показать кратко какие сценарии созданы;
2. обновить completed_orders_count для Анны;
3. убедиться, что метрики и канбаны начинают выглядеть «живыми».
