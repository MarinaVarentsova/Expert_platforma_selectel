Нужно провести полный аудит статусной модели заказов в проекте Палаты.

Сейчас ничего не исправляй.
Не меняй код.
Не меняй БД.
Не создавай commit.
Не делай push.

Цель:
получить фактическую статусную модель, которая сейчас заложена в production-коде, отдельно для дорожки заказчика и дорожки эксперта.

Проанализируй:

- artifacts/palata/server.js
- artifacts/palata/src/pages/CustomerDashboard.tsx
- artifacts/palata/src/pages/RequestDetail.tsx
- artifacts/palata/src/pages/ExpertDashboard.tsx
- artifacts/palata/src/lib/actionItems.ts
- artifacts/api-server/
- migrations/
- enum-типы PostgreSQL
- все SQL UPDATE/INSERT по статусам

Нужно исследовать минимум следующие сущности:

1. public.palata_requests.status
2. public.palata_request_matches.status
3. public.palata_request_contacts.expert_status
4. public.palata_action_items.status
5. public.palata_action_items.action_type
6. public.palata_status_events.new_status и old_status

Этап 1. Фактические значения статусов

Для каждой сущности покажи полный список допустимых или реально используемых значений.

Для enum получить значения из миграций/схемы.

Для TEXT-полей найти все строковые литералы в коде.

Составь таблицу:

сущность |
поле |
значение |
где объявлено |
где записывается |
где читается |
назначение

Этап 2. Дорожка заказчика

Построй полный жизненный цикл заказа с точки зрения заказчика.

Для каждого этапа показать:

- название статуса в БД;
- подпись в интерфейсе;
- в какой колонке хранится;
- событие, которое переводит заказ в статус;
- кто инициирует;
- endpoint;
- SQL;
- предыдущий статус;
- следующий статус;
- какие action items создаются или закрываются;
- что видит заказчик;
- что видит эксперт.

Минимально проверить этапы:

- новый заказ;
- автоподбор не запущен;
- идёт подбор;
- эксперты подобраны;
- выбор эксперта;
- эксперт выбран заказчиком;
- ожидание ответа эксперта;
- эксперт предложил дату;
- эксперт принял работу;
- эксперт отказался;
- повторный подбор;
- заказ в работе;
- заказ завершён;
- заказ отменён;
- заказ неактуален;
- заказ возвращён на подбор;
- заказ закрыт администратором.

Этап 3. Дорожка эксперта

Построй отдельный жизненный цикл того же заказа с точки зрения эксперта.

Для каждого этапа показать:

- статус match;
- expert_status в contacts;
- action_type;
- где карточка отображается:
  - Рынок;
  - Новые предложения;
  - Требуют действия;
  - Мои заказы;
  - В работе;
  - Выполненные;
- доступные кнопки;
- endpoint каждой кнопки;
- что меняется в каждой таблице;
- какое действие закрывается;
- какой статус заказа меняется.

Минимально проверить сценарии:

- эксперт получил предложение от автоподбора;
- заказчик выбрал эксперта;
- эксперт видит новое предложение;
- эксперт принимает в работу;
- эксперт указывает дату начала;
- эксперт отказывается;
- предложение истекает;
- эксперт начинает работу;
- эксперт завершает работу;
- заказчик оценивает эксперта.

Этап 4. Все переходы

Составь таблицу переходов:

роль |
действие |
endpoint |
palata_requests.status: было → стало |
palata_request_matches.status: было → стало |
palata_request_contacts.expert_status: было → стало |
созданный action_type |
закрытый action_type |
status event |
условия и guards

Этап 5. Несоответствия

Найди все места, где статусные домены смешаны или противоречат друг другу.

Особенно проверить:

- accepted_work в palata_request_matches.status;
- contacts_opened в palata_request_matches.status;
- completed в palata_request_matches.status;
- selected_by_customer;
- accepted;
- expert_selection;
- expert_selected_by_customer;
- customer_selected_you;
- you_are_approved_for_work;
- can-start;
- decline;
- take-work;
- complete.

Отдельно показать:

1. Статусы, которые пишутся, но нигде не читаются.
2. Статусы, которые читаются, но нигде не записываются.
3. Статусы, которые используются только во frontend.
4. Статусы, которые используются только в backend.
5. Дублирующие значения с разными названиями.
6. Условия рендера, которые не соответствуют backend-статусам.
7. Action items, которые создаются, но не отображаются.
8. Action items, которые отображаются, но не создаются.
9. Переходы, которые могут привести к зависшему состоянию.
10. Переходы, где одна таблица обновляется, а другая нет.

Этап 6. Итоговая модель

Верни две понятные схемы в текстовом виде:

А. Дорожка заказчика

Новый
→ ...
→ ...
→ Выполнен / Неактуален

Б. Дорожка эксперта

Предложение
→ Выбран заказчиком
→ Принял / Предложил дату / Отказался
→ В работе
→ Завершён

Для каждого перехода укажи точное значение в каждой таблице.

Этап 7. Приоритет проблем

Составь список проблем по критичности:

P0 — ломает переход или транзакцию;
P1 — заказ попадает не в тот раздел;
P2 — неверная подпись или кнопка;
P3 — устаревший код или неиспользуемый статус.

В конце вернуть только:

1. Полный список статусов по сущностям.
2. Дорожку заказчика.
3. Дорожку эксперта.
4. Таблицу переходов.
5. Все найденные несоответствия.
6. Приоритетный план исправлений.

Никаких изменений кода.
Никаких commit/push.