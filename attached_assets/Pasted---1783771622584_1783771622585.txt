Ничего не переписывать с нуля.

Очень важно:

Существующий импорт уже работает правильно.

НЕ трогать:

- чтение Excel;
- батчевую загрузку;
- INSERT в public.palata_certificates_import;
- INSERT в public.palata_certificate_import_logs;
- структуру текущего POST /api/palata/cert-import.

Эти части уже протестированы и работают.

=========================
Нужно сделать только одно
=========================

После того как полностью заполнена таблица

public.palata_certificates_import

необходимо восстановить отсутствующий последний этап старой ETL.

То есть нужно перенести только ту часть старой функции etl_process_certificate_import,
которая переносила данные

public.palata_certificates_import
        ↓
public.palata_certificates

Если после этого уже существующий код продолжает выполнять работу
с palata_certificate_specialty_codes,
palata_expert_certificates,
palata_expert_directions —
оставить его.

Если этих частей сейчас нет —
ПОКА ИХ НЕ ДЕЛАТЬ.

Сейчас задача только одна:

после завершения импорта staging заполнить

public.palata_certificates

=========================
Что должно происходить
=========================

После окончания загрузки в staging:

1. пройти по строкам public.palata_certificates_import;

2. выполнить ту же нормализацию, что была в старой ETL;

3. выполнить INSERT/UPDATE в

public.palata_certificates

с использованием новой схемы PostgreSQL;

4. вернуть статистику обратно существующему handler.

=========================
НЕ делать
=========================

НЕ переписывать существующий handler.

НЕ менять импорт.

НЕ менять фронтенд.

НЕ менять AdminCertImport.

НЕ переносить весь ETL целиком.

НЕ реализовывать пока:

- palata_certificate_specialty_codes
- palata_expert_certificates
- palata_expert_directions

Это будет отдельным этапом после проверки.

=========================
В отчёте показать

1. какой именно участок старой функции перенесён;

2. где он встроен;

3. какие SQL выполняются;

4. подтвердить, что остальной код импорта не изменялся.