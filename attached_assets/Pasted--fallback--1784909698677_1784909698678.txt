Стоп. Локальное определение направления и fallback по ключевым словам делать нельзя.

Требуемая логика проекта уже определена:

1. В коде существуют 31 допустимое направление экспертизы.
2. В коде или текущей конфигурации существует база знаний из 8 записей.
3. Описание новой заявки вместе с этой базой знаний и списком направлений должно отправляться в OpenAI.
4. OpenAI должен:

   * выбрать подходящее направление, например «Строительно-техническая экспертиза»;
   * либо вернуть предусмотренный текущей логикой результат «как есть», если подходящее направление определить нельзя.
5. Никакой самостоятельный локальный подбор по словам добавлять нельзя.

Сейчас проблема только в том, что запрос к OpenAI возвращает HTTP 403.

По логам известно:

```text
[AI-PROD] OPENAI_API_KEY при запуске: присутствует
[AI-PROD] Запрос получен
[AI-PROD] OPENAI_API_KEY присутствует = true
[AI-PROD] описание длина=59
[AI-PROD] Количество доступных маршрутов: 31
[AI-PROD] записей в базе знаний=8
[AI-PROD] отправляет запрос в OpenAI
[AI-PROD] Статус OpenAI HTTP = 403
[AI-PROD] возвращается код ошибки = 502
```

Это означает, что код доходит до вызова OpenAI. Но нужно проверить, корректно ли сформирован и разрешён сам запрос.

Работать только с:

* `artifacts/palata/server.js`;
* `lib/ai-detect/src/index.js`.

Не изменять:

* auth-service;
* frontend;
* БД;
* SQL;
* API endpoints;
* JSON-ответы;
* список из 31 направления;
* базу знаний из 8 записей;
* prompt и правила классификации без доказанной необходимости;
* архитектуру;
* успешную логику определения направления;
* модель, URL или headers до выяснения причины;
* создание заявки;
* другие файлы проекта.

## Что нужно сделать сейчас

Ничего нового не переписывать.

Проверить уже добавленные диагностические логи из commit `6018b4e` и установить точную причину HTTP 403.

Нужно выполнить production-запрос и вернуть фактические значения:

```text
[AI-PROD] OpenAI URL =
[AI-PROD] OpenAI model =
[AI-PROD] Authorization Bearer =
[AI-PROD] API key length =
[AI-PROD] API key trimmed length =
[AI-PROD] API key has surrounding whitespace =
[AI-PROD] OpenAI response.status =
[AI-PROD] OpenAI response.statusText =
[AI-PROD] OpenAI response.headers =
[AI-PROD] OpenAI response.body =
```

Сам API-ключ и любые его части не печатать.

Особое внимание уделить `OpenAI response.body`: именно там OpenAI возвращает код и текст реальной ошибки.

## Дополнительно проверить без изменения кода

1. Что в Coolify задеплоен именно commit `6018b4e`.
2. Что приложение запускается из:

```text
/app/artifacts/palata
node server.js
```

3. Что `OPENAI_API_KEY` задан в Environment Variables именно приложения Палата.
4. Что после изменения переменной выполнен полный Redeploy, а не только Restart.
5. Что в значении ключа нет:

   * кавычек;
   * пробела в начале;
   * пробела или переноса строки в конце;
   * префикса `Bearer `;
   * имени переменной вместо самого значения.
6. Что загружен действующий OpenAI API key, а не:

   * ChatGPT session token;
   * ключ другого сервиса;
   * старый удалённый ключ;
   * project key без необходимых прав.
7. Что запрос действительно уходит на:

```text
https://api.openai.com/v1/chat/completions
```

8. Что заголовок фактически формируется так:

```js
Authorization: `Bearer ${apiKey}`
```

и в него не попадает `undefined`, двойной `Bearer` или необрезанное значение.

## Важное ограничение

Пока не получен полный `OpenAI response.body`, никаких исправлений логики не вносить.

Не добавлять fallback.

Не отключать OpenAI.

Не подменять результат направлением по умолчанию.

Не разрешать создание заявки без существующего результата AI, если это меняет текущую бизнес-логику.

Сначала вернуть мне:

1. Полный диагностический фрагмент production-логов без секретов.
2. Точный текст и код ошибки из `OpenAI response.body`.
3. Вывод о причине 403, основанный именно на ответе OpenAI.
4. Минимальное предлагаемое исправление.

Код менять только после установления точной причины и только в случае, если проблема действительно находится в коде. Если проблема в ключе, правах проекта, регионе, аккаунте OpenAI или настройках Coolify — код не менять.
