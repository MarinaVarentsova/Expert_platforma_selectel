Пока не деплой и не создавай новый commit.

Ты повторно прислал описание уже выполненной реализации, но не выполнил запрошенный аудит полного покрытия.

Нужно сейчас проверить ВСЕ frontend-запросы к /api/palata/*.

1. Найди по всему проекту все:
- fetch(
- axios
- другие HTTP-клиенты

которые вызывают:
- /api/palata/*
- динамически сформированные URL к /api/palata/*

2. Верни точные цифры:
- сколько всего защищённых запросов к /api/palata/* найдено;
- сколько уже используют palataFetch;
- сколько всё ещё используют прямой fetch.

3. Верни таблицу оставшихся прямых запросов:

- файл;
- строка;
- endpoint;
- функция;
- защищённый или публичный;
- может ли получить 401 INVALID_TOKEN.

4. Особенно проверить:

- ExpertDashboard;
- CustomerDashboard;
- AdminDashboard;
- страницы заказа;
- карточку заявки;
- профиль эксперта;
- профиль клиента;
- маркет заявок;
- action items;
- загрузку файлов;
- скачивание файлов;
- polling;
- периодические обновления;
- useEffect-запросы непосредственно внутри React-компонентов.

5. Все защищённые вызовы /api/palata/*, которые могут получить 401, переведи с fetch на palataFetch.

При замене нельзя менять:
- URL;
- method;
- headers;
- body;
- обработку ответа;
- бизнес-логику;
- server.js;
- Auth Service;
- БД.

6. Важно проверить вызовы с Request и URL объектами, например:

fetch(new Request(...))
fetch(urlVariable)
fetch(`${baseUrl}/api/palata/...`)

7. После изменений снова выполни глобальный поиск и подтверди:

Все защищённые frontend-запросы к /api/palata/* используют palataFetch.

Если какие-либо прямые fetch оставлены, перечисли каждый и объясни причину.

8. Выполни:
- build;
- все тесты.

9. Добавь изменения в уже существующий commit:

fix: handle expired sessions

Не создавай отдельный commit.

В финальном ответе не повторяй старое описание механизма. Верни именно:

- итог поиска;
- количество найденных вызовов;
- количество замен;
- список оставленных прямых fetch;
- build;
- tests;
- hash обновлённого commit.