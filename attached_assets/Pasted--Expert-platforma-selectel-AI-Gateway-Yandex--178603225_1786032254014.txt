Работаем только в репозитории Expert_platforma_selectel.

Интеграция Палата → AI Gateway → Yandex работает:
- HTTP 200;
- JSON успешно разобран;
- matched_markers приходят.

Фактическая ошибка:

Yandex вернул:
direction_name = "Некачественный ремонт"

Но в справочнике Палаты допустимое направление называется:
"Строительно-техническая экспертиза"

Из-за отсутствия точного совпадения код выставляет:
detected=false
rejectionCode=AI_NOT_DETECTED

Нужно исправить только контракт выбора направления.

1. Найди формирование system prompt в lib/ai-detect/src/index.js.

2. Передавай модели полный список разрешённых направлений из текущего справочника проекта.

3. В system prompt жёстко зафиксируй:

- direction_name может быть только точным значением из переданного списка;
- нельзя придумывать категории, подкатегории и бытовые названия;
- нельзя возвращать:
  "Некачественный ремонт",
  "Дефекты отделки",
  "Строительные дефекты";
- для дефектов ремонта, плитки, отделки и качества строительных работ возвращать точное название:
  "Строительно-техническая экспертиза";
- если ни одно направление не подходит:
  detected=false,
  direction_name=null.

4. Формат ответа оставить:

{
  "detected": true,
  "direction_name": "точное название из списка",
  "confidence": 0.95,
  "reason": "...",
  "matched_markers": []
}

5. Добавить серверную нормализацию перед поиском направления.

Создать таблицу допустимых алиасов минимум:

"некачественный ремонт"
"дефекты ремонта"
"дефекты отделки"
"строительные дефекты"
"ремонтные работы"
→ "Строительно-техническая экспертиза"

Сопоставление:
- trim;
- lowercase;
- заменить ё на е;
- точное совпадение с официальным названием;
- затем поиск по алиасам;
- никаких нечётких совпадений между произвольными направлениями.

6. После нормализации:

если исходное значение:
"Некачественный ремонт"

результат должен быть:
directionName = "Строительно-техническая экспертиза"
directionId = существующий UUID этого направления
detected = true

7. Не менять:
- AI Gateway;
- Yandex API;
- Auth Service;
- frontend;
- локальные стоп-факторы;
- базу знаний;
- структуру БД;
- алгоритм создания заказа.

8. Добавить тесты:

А.
direction_name="Некачественный ремонт"
→ "Строительно-техническая экспертиза"
→ detected=true.

Б.
direction_name="Дефекты отделки"
→ то же направление.

В.
direction_name="Строительно-техническая экспертиза"
→ остаётся без изменений.

Г.
неизвестное значение
→ безопасный fallback, без ошибочного назначения.

Д.
detected=false и direction_name=null
→ остаётся отклонением.

9. Запустить:
- новые тесты;
- существующие 48 тестов;
- tsc --noEmit;
- vite build.

10. Commit:
fix: map AI categories to official directions

Push в main.

Вернуть:
- изменённые файлы;
- diff нормализации;
- результаты тестов;
- hash commit;
- подтверждение push.