КРИТИЧЕСКАЯ ЗАДАЧА. Нужно доделать загрузчик реестра сертификатов так, чтобы администратор зашёл в админку Палаты, загрузил Excel-файл с 4 полями, и система сама полностью обновила все нужные таблицы по текущей логике платформы.

Сейчас сделано недостаточно:
- создана страница AdminCertImport.tsx;
- добавлена вкладка «Сертификаты»;
- создан маршрут /admin/cert-import;
- создана таблица public.palata_certificates_import;
- создана RPC truncate_certificates_import();
- Excel загружается только в palata_certificates_import.

Этого мало.

Мне нужен полноценный загрузчик реестра сертификатов в админке Палаты.

РАЗМЕЩЕНИЕ В ИНТЕРФЕЙСЕ

Загрузчик должен быть доступен только администратору:

Админка Палаты
→ вкладка «Сертификаты»
→ страница «Импорт реестра сертификатов»

Если текущий маршрут уже /admin/cert-import — оставить его, но он должен быть нормально доступен из меню админки.

На странице должно быть:

1. Блок «Текущий реестр»
- всего сертификатов в import;
- активных;
- истёкших;
- дата последней загрузки;
- количество привязанных к зарегистрированным экспертам;
- количество не привязанных к экспертам.

2. Блок загрузки Excel:
- drag & drop;
- кнопка выбора файла;
- поддержка .xlsx и .xls;
- файл парсится в браузере.

3. Предпросмотр перед импортом:
- всего строк;
- активных;
- истёкших;
- ошибок даты;
- пустых номеров;
- пустых ФИО;
- первые 20 строк.

4. Перед импортом обязательное предупреждение:

«Загрузка нового файла полностью заменит текущий реестр сертификатов. Продолжить?»

Кнопки:
- Отмена
- Продолжить импорт

5. После импорта показать итог:
- загружено строк в import;
- активных;
- истёкших;
- создано/обновлено записей в palata_certificates;
- создано/обновлено связей с кодами;
- создано/обновлено связей с направлениями;
- привязано к зарегистрированным экспертам;
- не найдено экспертов;
- ошибок парсинга.

---

ВХОДНОЙ EXCEL

Я отдаю Excel с 4 полями:

1. Номер документа
2. ФИО эксперта
3. Область производства судебной экспертизы
4. Срок действия сертификата

Пример:
- Номер документа → certificate_number
- ФИО эксперта → expert_full_name
- Область производства судебной экспертизы → specialty_text / codes / directions
- Срок действия сертификата → certificate_period / valid_from / valid_to

---

ОБЯЗАТЕЛЬНАЯ ЛОГИКА ИМПОРТА

После загрузки Excel система должна сделать весь ETL сама:

1. Очистить import-таблицу:
public.palata_certificates_import

2. Загрузить все строки Excel в:
public.palata_certificates_import

3. Распарсить:
- certificate_number
- expert_full_name
- specialty_text
- certificate_period
- codes
- directions
- valid_from
- valid_to
- certificate_status
- load_status

4. Рассчитать certificate_status:
- valid_to >= current_date → "Активный"
- valid_to < current_date → "Истёкший"
- дата не распарсилась → "Ошибка даты"

5. load_status:
- "Загружен" — если строка корректно загружена;
- "Ошибка даты" — если дата не распарсилась;
- "Нет номера сертификата" — если пустой номер;
- "Нет ФИО эксперта" — если пустое ФИО;
- "Ожидает регистрации эксперта" — если эксперт не найден в зарегистрированных пользователях.

6. Грузить ВСЕ сертификаты:
- действующие;
- истёкшие.

Ничего не фильтровать по сроку действия на этапе импорта.

---

РАБОЧИЕ ТАБЛИЦЫ, КОТОРЫЕ НУЖНО ЗАПОЛНЯТЬ

После загрузки в import нужно разнести данные по рабочим таблицам текущей платформы.

Минимально должны обновляться:

1. public.palata_certificates_import
2. public.palata_certificates
3. public.palata_certificate_specialty_codes
4. public.palata_expert_certificates — если эксперт найден в системе
5. public.palata_expert_directions — если эксперт найден и направление определено
6. public.palata_speciality_codes / public.palata_specialty_codes — проверить точное название таблицы в проекте
7. public.palata_expertise_directions — использовать только существующий справочник, не создавать дубли направлений

Перед реализацией проверить фактические названия таблиц и полей в Supabase/коде. Не выдумывать имена.

Если в проекте таблица называется palata_speciality_codes, использовать её.
Если palata_specialty_codes — использовать её.
Вернуть в отчёте точное фактическое название.

---

СОПОСТАВЛЕНИЕ НАПРАВЛЕНИЙ

Поле "Область производства судебной экспертизы" может содержать текст и коды, например:

"16.1 – 16.6 ССТЭ"
"24.4. Исследование экологического состояния..."
"7.3. Исследование видеоизображений..."

Нужно:

1. Извлечь коды:
- 16.1
- 16.2
- 16.3
- 16.4
- 16.5
- 16.6
и другие.

2. Определить направление по текущему справочнику:
public.palata_expertise_directions

3. Не создавать новые направления без необходимости.
Использовать только существующие направления.

4. Если направление не удалось определить:
- сохранить строку в import;
- не ломать импорт;
- показать в отчёте как "не определено направление".

---

ПОИСК ЭКСПЕРТА

По expert_full_name нужно найти зарегистрированного эксперта.

Проверить текущую схему и использовать существующую логику поиска.

Минимальная логика:

1. Искать в public.palata_users:
- role = 'expert'
- совпадение full_name с expert_full_name

2. Совпадение ФИО делать нормализованным:
- trim;
- lower;
- заменить множественные пробелы на один;
- сравнивать без учета регистра.

3. Если эксперт найден:
- создать/обновить сертификат в public.palata_expert_certificates;
- создать/обновить направления эксперта в public.palata_expert_directions;
- если есть таблица регионов, её не трогать.

4. Если эксперт не найден:
- данные остаются в общем реестре;
- в import load_status = "Ожидает регистрации эксперта";
- в palata_expert_certificates запись не создавать, потому что нет user_id.

5. При будущей регистрации эксперта должна быть возможность подтянуть сертификаты из реестра по ФИО.
Если такой логики нет — добавить задачу/функцию или явно вернуть, что это пока не реализовано.

---

СТАТУСЫ СЕРТИФИКАТОВ В РАБОЧИХ ТАБЛИЦАХ

Для palata_expert_certificates:

Если certificate_status = "Активный":
- status = "verified" или фактическое активное значение enum/текущей логики проекта.

Если certificate_status = "Истёкший":
- status = "expired" / "inactive" / другое фактическое значение по текущей схеме.
- Если enum не поддерживает expired, не ломать схему, а вернуть SQL/миграцию на согласование.

Истёкшие сертификаты должны храниться, но НЕ должны давать эксперту допуск к matching.

Проверить, что matching использует:
- status = verified;
- cert_valid_to >= current_date;
- направление сертификата соответствует заказу.

---

ЗАЩИТА ОТ ДУБЛЕЙ

Нельзя создавать дубли при повторной загрузке одного файла.

Уникальность минимум:

certificate_number + expert_full_name + valid_to

Если в текущей схеме есть более правильный ключ — использовать его и описать.

При повторной загрузке:
- существующие записи обновлять;
- новые создавать;
- дубли не плодить.

---

ПОЛНАЯ ПЕРЕЗАПИСЬ

Важно:

public.palata_certificates_import — это слепок последнего полного файла.
Перед каждой загрузкой import очищается полностью.

Но рабочие таблицы нельзя бездумно очищать, если они используются системой.

Для рабочих таблиц использовать upsert/обновление по ключам.

Не удалять:
- экспертов;
- пользователей;
- заказы;
- регионы;
- справочники направлений;
- справочники специальностей.

---

БЕЗОПАСНОСТЬ И ПРАВА

Загрузчик доступен только admin.

Если пользователь не admin:
- не показывать вкладку;
- не давать открыть /admin/cert-import.

RPC-функции:
- если нужен TRUNCATE, использовать SECURITY DEFINER;
- ограничить выполнение только для админа;
- не давать обычным пользователям очищать таблицы.

---

ЖУРНАЛ ЗАГРУЗОК

Создать или использовать таблицу журнала, например:
public.palata_certificate_import_logs

Поля:
- id
- created_at
- created_by
- file_name
- total_rows
- active_count
- expired_count
- parse_error_count
- linked_experts_count
- unlinked_experts_count
- status
- error_message

После каждого импорта писать запись в журнал.

На странице «Сертификаты» показывать последние 10 загрузок.

---

КРИТЕРИИ ПРИЁМКИ

Я должна зайти:

Админка Палаты
→ Сертификаты
→ загрузить Excel

И после загрузки без ручных SQL получить:

1. palata_certificates_import очищена и заполнена новым файлом.
2. palata_certificates обновлена.
3. Связи сертификатов с кодами созданы/обновлены.
4. Связи с направлениями созданы/обновлены.
5. Для зарегистрированных экспертов созданы/обновлены palata_expert_certificates.
6. Для зарегистрированных экспертов обновлены palata_expert_directions.
7. Истёкшие сертификаты загружены и сохранены.
8. Истёкшие сертификаты не участвуют в matching.
9. Дубли не создаются.
10. В админке виден отчет импорта.
11. В админке виден журнал последних загрузок.
12. Typecheck/build проходит.
13. Vercel deploy не падает.

---

ОБЯЗАТЕЛЬНО ВЕРНУТЬ ОТЧЁТ

После реализации вернуть:

1. Где находится загрузчик в интерфейсе.
2. Точный маршрут.
3. Какие файлы изменены.
4. Какие таблицы созданы/изменены.
5. Какие RPC-функции созданы/изменены.
6. Какие рабочие таблицы реально заполняются.
7. Карта полей:
   Excel → import → рабочие таблицы.
8. Как определяется направление.
9. Как определяется код специальности.
10. Как ищется зарегистрированный эксперт.
11. Что происходит, если эксперт не найден.
12. Что происходит, если сертификат истёк.
13. Как предотвращаются дубли.
14. Как matching защищён от истёкших сертификатов.
15. Результат тестового импорта минимум на 5 строках.
16. Результат typecheck/build.

НЕ делать:
- не ломать регистрацию;
- не трогать OpenAI;
- не трогать заказы;
- не трогать matching кроме проверки истёкших сертификатов;
- не удалять пользователей;
- не удалять экспертов;
- не создавать дубли справочников;
- не делать отдельный непонятный загрузчик вне админки.