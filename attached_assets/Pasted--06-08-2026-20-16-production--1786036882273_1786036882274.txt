[06.08.2026 20:16] Анна: Нужно закончить миграцию production-платформы и убрать пять оставшихся технических хвостов.

ВАЖНО:
— не менять продуктовую логику платформы;
— не переписывать интерфейс;
— не менять логику AI-классификатора;
— не менять UniSender;
— не делать лишний рефакторинг;
— изменения должны быть минимальными и только по перечисленным ниже пунктам;
— все изменения делать именно в production-коде, а не просто скрывать UI;
— после каждого блока проверить, что основной функционал платформы не сломан.

Перед началом коротко покажи, какие файлы/модули будут затронуты.
После выполнения дай итоговый технический отчёт по контрольному списку в конце.

==================================================
1. ПОЛНОСТЬЮ УБРАТЬ SUPABASE ИЗ PRODUCTION
==================================================

По последнему аудиту Supabase всё ещё активно используется для:
— palata_action_items;
— palata_status_events;
— matcher/scheduler;
— файлов экспертных документов;
— Supabase auth session palata_auth в localStorage.

Нужно полностью вывести Supabase из production.

1.1. palata_action_items

Сейчас frontend напрямую пишет данные через:
supabase.from("palata_action_items")

Перевести ВСЕ операции:
— insert;
— select;
— update;
— resolve;
— cancel;
— load;

на наш backend API и PostgreSQL Selectel.

Frontend не должен напрямую обращаться к БД.

1.2. palata_status_events

Сейчас часть INSERT идёт напрямую через:
supabase.from("palata_status_events")

Перевести запись и чтение полностью на backend + PostgreSQL Selectel.

1.3. Matcher / scheduler

Проверить api-server matcher/scheduler.

Если он использует Supabase client:
— перевести его на PostgreSQL Selectel;
— сохранить текущую логику матчинга;
— не менять алгоритм подбора экспертов.

После изменения matcher/scheduler не должен импортировать или инициализировать Supabase.

1.4. Документы экспертов

Сейчас файлы экспертных документов хранятся в Supabase Storage bucket:
palata-expert-documents

Перенести файловое хранение на Selectel Object Storage.

Требования:
— bucket приватный;
— постоянные публичные URL не использовать;
— загрузка через backend либо безопасный signed URL;
— пользователь может получить файл только после проверки прав доступа;
— metadata файлов остаются в PostgreSQL Selectel;
— сохранить текущий UI загрузки документов эксперта;
— сохранить удаление документов;
— при удалении записи должен удаляться и физический объект из Selectel Object Storage.

Нужно сохранить:
— file_name;
— mime_type;
— size_bytes;
— bucket_path/object_key;
— owner/expert_id;
— created_at.

1.5. Исторический bucket palata-request-files

По последнему аудиту новые файлы заявок туда больше не загружаются, но старые объекты физически остались.

НЕ удаляй их автоматически без проверки.

Сначала:
— посчитай количество объектов;
— установи, есть ли ссылки на них в palata_request_files;
— установи, доступны ли они через текущий UI/API.

В итоговом отчёте напиши, сколько исторических файлов осталось и можно ли их безопасно удалить отдельной операцией.

1.6. Supabase SDK

После переноса всего функционала:

— удалить production-импорты @supabase/supabase-js;
— удалить supabaseClient;
— удалить использование VITE_SUPABASE_URL;
— удалить использование VITE_SUPABASE_ANON_KEY;
— удалить palata_auth из localStorage;
— убрать Supabase из production dependencies, если больше нигде не используется.

Проверить production bundle поиском по:
supabase.co
supabase
VITE_SUPABASE
palata_auth

В production не должно остаться ни одного исполняемого обращения к Supabase.

==================================================
2. УБРАТЬ ВНЕШНИЕ GOOGLE FONTS
==================================================

Сейчас index.html грузит Inter с:
— fonts.googleapis.com
— fonts.gstatic.com

Нужно:

— убрать эти <link>;
— разместить используемые начертания Inter локально в проекте;
— подключить их через локальный CSS/@font-face;
— убедиться, что внешний вид интерфейса не изменился.

После build проверить production bundle/index.html.

При открытии сайта браузер не должен делать запросы на:
— fonts.googleapis.com
— fonts.gstatic.com.

==================================================
3. УДАЛИТЬ VERCEL FALLBACK ИЗ AI
==================================================

Текущий production AI работает через российский gateway на Selectel и Yandex AI Studio.

Но в коде остался hardcoded fallback:

https://ai-gateway-core.vercel.app/api/chat

Его нужно полностью удалить.

Требование:

Если AI_GATEWAY_URL отсутствует или некорректен:
— запрос НЕ должен автоматически уходить на Vercel;
— запрос НЕ должен уходить на OpenAI или другой иностранный сервис;
— AI-классификатор должен вернуть контролируемую ошибку;
— можно использовать существующий локальный fallback checkLocalMarkers(), если он уже предусмотрен текущей логикой.

Убрать из production-кода:
— ai-gateway-core.vercel.app;
— любые другие Vercel fallback URL;
— любые прямые OpenAI URL/SDK, если они остались.

Не менять текущую логику system prompt, базы знаний и классификации Alice/Yandex.

Отдельно убрать старое техническое имя gpt-4o-mini, если оно используется только как исторический label и не определяет фактическую Yandex-модель.

Не менять фактическую модель без отдельной команды.

==================================================
4. ИСПРАВИТЬ AUTH: НЕ ПЕРЕДАВАТЬ ПАРОЛИ ПО ПУБЛИЧНОМУ HTTP
==================================================

Сейчас backend обращается к:

http://q1rwqqgfbmvyhwgsdr701t0h.161.104.50.164.sslip.io

Через этот канал передаются:
— email;
— password;
— full_name;
— phone;
— reset token.

Оба сервиса находятся на Selectel.

Нужно убрать передачу этих данных по публичному HTTP.

Приоритетный вариант:

1. Если сервисы могут общаться по internal/private network Selectel:
— использовать внутренний адрес;
— не выводить auth-трафик в публичную сеть.

Если это невозможно:

2. Перевести AUTH_SERVICE_URL на HTTPS с валидным TLS.

После изменения:
— register работает;
— login работает;
— forgot-password работает;
— reset-password работает;
— /api/auth/me работает;
— текущая Bearer-token схема не ломается.

Не менять сейчас способ хранения access token в localStorage — это отдельная задача.

Дополнительно проверь код самого AUTH_SERVICE, если доступен:
— какой hash используется для паролей;
— plaintext пароль нигде не хранится;
— plaintext пароль не логируется.

В отчёте назвать hash-алгоритм.

==================================================
5. ДОБАВИТЬ ТЕХНИЧЕСКИЙ МЕХАНИЗМ УДАЛЕНИЯ / ОБЕЗЛИЧИВАНИЯ ПД
==================================================

Не нужно делать пользовательский красивый интерфейс «Удалить аккаунт».

Нужен административный backend-сценарий, позволяющий выполнить требование на удаление ПД конкретного пользователя.

Реализовать admin-only endpoint или административную команду:

delete/anonymize user by user_id

Перед выполнением обязательно проверять административные права.

Для ЗАКАЗЧИКА обработать связанные данные:

— palata_users;
— palata_customer_profiles;
— palata_requests;
— palata_request_matches;
— palata_request_contacts;
— palata_status_events;
— palata_action_items;
— palata_customer_ratings;
— palata_email_events;
— связанные file metadata;
— связанные физические файлы в Selectel Object Storage.

Для ЭКСПЕРТА:

— palata_users;
— palata_expert_profiles;
— palata_expert_regions;
— palata_expert_directions;
— palata_expert_certificates;
— palata_expert_documents;
— связанные файлы в Selectel Object Storage;
— palata_request_matches;
— palata_request_contacts;
— palata_status_events;
— palata_action_items;
— palata_customer_ratings;
— palata_email_events.

Если физическое удаление отдельных записей нарушит целостность истории или foreign keys:

разрешается обезличивание:
— ФИО → NULL / техническое значение;
— email → NULL / технический уникальный идентификатор;
— phone → NULL;
— свободные notes/comments → удалить или обезличить;
— references оставить только если они больше не позволяют определить человека.

Не уничтожать бизнес-данные, которые можно сохранить после необратимого обезличивания.

Сделать режим DRY RUN:

dry_run=true

Он должен показать:
— какие таблицы будут затронуты;
— сколько записей;
— сколько файлов;
— что будет deleted;
— что будет anonymized.
[06.08.2026 20:16] Анна: Только при dry_run=false выполнять реальные изменения.

Операцию логировать:
— кто инициировал;
— user_id;
— дата;
— результат;
— количество удалённых/обезличенных записей.

В лог НЕ писать удаляемые персональные данные.

==================================================
6. НИЧЕГО НЕ МЕНЯТЬ В ЭТИХ ЧАСТЯХ
==================================================

Не менять:
— Yandex AI Studio / Alice AI LLM Flash;
— текущий AI Gateway на Selectel;
— system prompt классификатора;
— базу знаний классификатора;
— UniSender;
— бизнес-логику заявок;
— matching rules;
— роли пользователей;
— логику сертификации экспертов;
— UI, кроме технически необходимых изменений для файлов.

==================================================
7. КОНТРОЛЬ ПОСЛЕ ИЗМЕНЕНИЙ
==================================================

После всех изменений:

1. Запусти unit/integration tests.
2. tsc --noEmit.
3. production build.
4. Выполни поиск по production-коду и production bundle.

Проверить строки:

supabase
supabase.co
VITE_SUPABASE
palata_auth
vercel.app
ai-gateway-core
openai
fonts.googleapis.com
fonts.gstatic.com

Для каждого результата указать:
— найден / не найден;
— если найден — где;
— LIVE / DEAD CODE / DEV ONLY.

==================================================
8. ФИНАЛЬНЫЙ ОТЧЁТ
==================================================

После выполнения дай короткий технический отчёт:

### SUPABASE
Production-вызовы Supabase: ДА/НЕТ

### STORAGE
Файлы экспертов хранятся: ...
Bucket приватный: ДА/НЕТ
Публичные постоянные URL: ДА/НЕТ

### HISTORICAL REQUEST FILES
Количество старых файлов в Supabase request bucket: ...
Можно безопасно удалить: ДА/НЕТ/НЕ УСТАНОВЛЕНО

### VERCEL
Fallback на Vercel полностью удалён: ДА/НЕТ

### OPENAI
Production-вызовы OpenAI: ДА/НЕТ

### GOOGLE
Внешние Google Fonts полностью удалены: ДА/НЕТ

### AUTH
AUTH transport: PRIVATE / HTTPS / HTTP
Пароль передаётся только по защищённому каналу: ДА/НЕТ
Hash алгоритм: ...

### DELETE WORKFLOW
Admin delete/anonymize workflow реализован: ДА/НЕТ
Dry run реализован: ДА/НЕТ
Физические файлы удаляются: ДА/НЕТ

### PRODUCTION DATA FLOW
Короткая схема текущего состояния:

browser → backend → DB
browser/backend → storage
backend → AI
backend → email
backend → auth

С конкретными provider/hostname.

### ОСТАВШИЕСЯ ВНЕШНИЕ СЕРВИСЫ
Перечислить только LIVE production-интеграции.

### ОСТАВШИЕСЯ ХВОСТЫ
Перечислить всё, что ещё осталось от старой архитектуры, даже если это DEAD CODE.

Не делай дополнительные улучшения после выполнения этого плана без отдельного согласования.