Следующий этап разработки MVP «Палата судебных экспертов».

Текущая ситуация:

* основные таблицы Supabase созданы;
* тестовые данные есть в `palata_requests`, `palata_expert_profiles`, `palata_request_matches`;
* `palata_users` пока пустая, авторизацию пока не трогаем.

Нужно сделать 2 блока:

1. Канбан-доски

Для `/customer`:
раздел «Мои заказы»
Колонки:

* Новый
* Идёт подбор
* Выбор эксперта
* В работе
* Выполнен
* Неактуален

Для `/expert`:
раздел «Мои обращения»
Колонки:

* Новые предложения
* Контакты открыты
* Могу взять
* В работе
* Завершено
* Отказ / не взял

Для `/admin`:
раздел «Все заказы»
Колонки:

* Новые
* Идёт подбор
* Выбор эксперта
* В работе
* Проблемные
* Выполненные
* Неактуальные

Требования:

* пока без авторизации;
* пока без drag-and-drop;
* карточки группировать по статусам из Supabase;
* если колонка пустая — всё равно показывать колонку;
* карточка заказа должна открываться в детальную страницу заказа;
* не использовать моковые данные.

2. Отдельная страница метрик для администратора

Добавить маршрут:

`/admin/metrics`

Название страницы:
«Метрики заказов»

На первом этапе собрать простой dashboard из данных Supabase.

Показатели:

* всего заказов;
* новые заказы;
* заказы в подборе;
* заказы на этапе выбора эксперта;
* заказы в работе;
* выполненные заказы;
* неактуальные заказы;
* всего экспертов;
* активные эксперты;
* эксперты с регистрацией в Палата судебных экспертов РФ;
* эксперты с регистрацией в Центр судебных экспертиз;
* средний рейтинг экспертов;
* средний рейтинг заказчиков;
* количество отказов экспертов;
* количество заказов, где все эксперты отказались;
* количество заказов по регионам;
* количество заказов по направлениям экспертиз.

Важно:

* страница метрик пока только читает данные;
* ничего не редактирует;
* если данных нет, показывать 0;
* использовать реальные таблицы Supabase;
* подготовить код так, чтобы позже можно было добавить графики.
